import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks'
import LinkTo from '@storybook/addon-links/react';
import StoryRouter from 'storybook-react-router';

import UsersManagementTemplate from '../UsersManagementTemplate'

import data from '../../../../__mocks__/components/UserTableMock.js';

import {
  USERS_TABLE_REFERENCE,
} from '../../../../schemas/references'

<Meta
  title='Components|Templates/UsersManagementTemplate'
  component={UsersManagementTemplate}
  decorators={[StoryRouter()]}
/>

# Users Management Template

Table to manage the list of users by an administrator.

<Preview>
  <Story name='Users Management Template'>
    <UsersManagementTemplate
      data={data}
      linkToCreateNewUser='/users/new/'
      fnImportUsers={() => alert('Import users to file .CSV')}
      linkToViewUser='/users/view/'
      linkToEditUser='/users/create/'
      fnDeleteUser={(id) => alert(`Do you want to delete the user ${id}`)}
    />
  </Story>
</Preview>

## Props

<Props of={UsersManagementTemplate} />

### Schemas

-  <LinkTo kind={USERS_TABLE_REFERENCE.path}>{USERS_TABLE_REFERENCE.name}</LinkTo>
