import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks'
import LinkTo from '@storybook/addon-links/react';
import StoryRouter from 'storybook-react-router';

import UsersManagementT from '../UsersManagementT'

import { data, menu } from './UsersManagementT.stories.aux'

import {
  USERS_TABLE_REFERENCE,
} from '../../../../schemas/references'

<Meta
  title='Components|Templates/UsersManagementT'
  component={UsersManagementT}
  decorators={[StoryRouter()]}
/>

# Users Management Template

Table to manage the list of users by an administrator.

<Preview>
  <Story name='Users Management Template'>
    <UsersManagementT
      data={data}
      menu={menu()}
      linkToViewUser='/view/user/'
      linkToUserEdit='/edit/user/'
      fnUserDelete={(id) => alert(`Do you want to delete the user ${id}`)}
    />
  </Story>
</Preview>

## Props

<Props of={UsersManagementT} />

### Schemas

-  <LinkTo kind={USERS_TABLE_REFERENCE.path}>{USERS_TABLE_REFERENCE.name}</LinkTo>
