import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks';
import StoryRouter from 'storybook-react-router';
import ProviderMock from '../../../../__mocks__/redux/ProviderMock'
import Navbar from '../Navbar';
import Surface from '../../../atoms/Surface/Surface'

<Meta
  title='Components|Organisms/Navbar'
  component={Navbar}
/>

# Navbar

This body is used to show the main actions of each role, in this case we're looking the admin role.

<Preview>
    <Story name='is Administrator who view info' decorators={[StoryRouter()]}>
      <ProviderMock store={{ user: { user: { role: 'admin' } } }}>
        <Surface>
          <Navbar isAdmin={true}/>
        </Surface>
      </ProviderMock>
    </Story>
</Preview>

In this other case we're looking no admin role.

<Preview>
    <Story name='is User who view info' decorators={[StoryRouter()]}>
      <ProviderMock store={{ user: { user: { role: 'patient' } } }}>
        <Surface>
          <Navbar />
        </Surface>
      </ProviderMock>
    </Story>
</Preview>

## Props

<Props of={Navbar} />
