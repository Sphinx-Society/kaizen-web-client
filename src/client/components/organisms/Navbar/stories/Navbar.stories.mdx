import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks'

import Navbar from '../Navbar'
import './Navbar.stories.scss';

<Meta
  title='Components|Organisms/Navbar'
  component={Navbar}
/>

# Navbar

XXXXX This organism is used to send data from a fixed list of options

## Default

<Preview>
  <div className='container-body'>
    <Navbar onClickSearch={() => null} onClickProfile={() => null} onClickSettings={() => null} />
  </div>
</Preview>

## With a parent container

<Preview>
  <div className='container-body'>
    <div className='container-page ssk--boxShadow'>
      <div className='container-page__element'>Elemento 1</div>
      <div className='container-page__element'>Elemento 2</div>
    </div>
    <Navbar onClickSearch={() => alert('Search')} onClickProfile={() => alert('MyProfile')} onClickSettings={() => alert('Settings')} />
  </div>
</Preview>

## Props

<Props of={Navbar} />
