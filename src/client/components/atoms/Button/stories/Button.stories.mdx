import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks'
import { FaEdit } from 'react-icons/fa';
import { MdKeyboardArrowLeft } from 'react-icons/md';
import { TiUserAdd } from 'react-icons/ti';



import Button from '../Button'
import './Button.stories.scss'

<Meta
  title='Components|Atoms/Button'
  component={Button}
/>

# Button

## Kaizen have two types of Button

<Preview>
  <div className='preview-flex1'>
    <Button
      color='primary'
      onClick={() => alert('It is a Button')}
    >
      Base button
    </Button>
    <Button
      type='icon'
      onClick={() => alert('It is an Icon Button')}
    >
      <FaEdit size='2em' />
    </Button>
  </div>
</Preview>

### Mode text or other components

<Preview>
  <div className='preview-grid1'>
    <Button color='primary'>width: auto</Button>
    <Button color='primary'>width: auto</Button>
  </div>
</Preview>

<Preview>
  <div className='preview-grid2'>
    <Button>Default color</Button>
    <Button color='primary'>Primary</Button>
    <Button color='secondary'>Secondary</Button>
    <Button color='warning'>Warning</Button>
    <Button disabled>Disabled</Button>
    <Button color='primary'>
      With emoji
      <span style={{padding: 0, margin: 0}}>😀</span>
    </Button>
  </div>
</Preview>

*__You can also overwrite it styles by adding a class__*

<Preview>
  <Button className='btn--stories'>
    With custom styles
    <span style={{padding: 0, margin: 0}}>
      😀
    </span>
  </Button>
</Preview>

### Mode Icon

<Preview>
  <Button type='icon'>
    <FaEdit size='3em' />
  </Button>
  <Button type='icon' color='primary'>
    <TiUserAdd size='3em' />
  </Button>
  <Button type='icon' color='secondary'>
    <FaEdit size='3em' />
  </Button>
  <Button type='icon' color='warning'>
    <MdKeyboardArrowLeft size='3em' />
  </Button>
  <Button type='icon' color='light'>
    <FaEdit size='3em' />
  </Button>
  <Button type='icon' onClick={() => alert('It is a Button')} disabled>
    <MdKeyboardArrowLeft size='3em' />
  </Button>
</Preview>

## Props

<Props of={Button} />
