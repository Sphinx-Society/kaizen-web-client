import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks'
import Button from '../Button'
import './Button.stories.scss'

<Meta
  title='Components|Atoms/Button'
  component={Button}
/>

# Button

## Kaizen have two types of button, you can add them text and other components

<Preview>
  <Button>Primary</Button>
  <Button secondary>Secondary</Button>
  <Button disabled>Disabled</Button>
  <Button>With emoji <span style={{padding: 0, margin: 0}}>😀</span></Button>
</Preview>

## You can also overwrite it styles by adding a class

<Preview>
  <Button className="btn--custom">With custom styles <span style={{padding: 0, margin: 0}}>😀</span></Button>
</Preview>


## Props

<Props of={Button} />