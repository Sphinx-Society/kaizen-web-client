import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks'

import Alert from '../Alert'

<Meta
  title='Components|Atoms/Alert'
  component={Alert}
/>

# Alert

This atom is intended to give feedback to the user about certain particular actions.

The function to close the alert is executed if the close icon is clicked or outside the alert figure.

<Preview>
  <Story name='Alert with a type success'>
    <div style={{height: '100px'}}>
      <Alert
        type='success'
        message='It is Ok'
        onClose={() => console.info('Function to close <Alert /> success')}
        closeTime={3000}
      />
    </div>
  </Story>
</Preview>

<Preview>
  <Story name='Alert with a type error'>
    <div style={{height: '100px'}}>
      <Alert
        type='error'
        message='This is an error'
        onClose={() => console.info('Function to close <Alert /> error')}
        closeTime={4000}
      />
    </div>
  </Story>
</Preview>

<Preview>
  <Story name='Alert with a type warning'>
    <div style={{height: '100px'}}>
      <Alert
        type='warning'
        message='Warning'
        onClose={() => console.info('Function to close <Alert /> warning')}
        closeTime={5000}
      />
    </div>
  </Story>
</Preview>
